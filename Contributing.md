# 贡献指南
* [1. 提交信息标准化](#1-提交信息标准化)
  + [1.1. 格式](#11-格式)
  + [1.2. 示例](#12-示例)

## 1. 提交信息标准化

> [!IMPORTANT]
> 如果**不使用**此提交信息规范提交更改，您的提交极有可能会被**直接忽略**，如果想要为仓库贡献，那么一定要**遵循此命名规则**

此项目采用行业规范提交信息

### 1.1. 格式

单行:

```
<type>(<scope>?): <subject>
```

多行:

```
<type>(<scope>?): <subject>

<body>

<footer>?
```

*带 `?` 的为可选项; `:` 为英文冒号, 与 `<subject>` 之间应有一个空格*

`type` - 前缀, 表明提交的类型，在 `type` 后添加 `!` 表示破坏性更改:
 * `feat` - 新增功能
 * `imp` - 改善某处代码
 * `fix` - 修复BUG
 * `docs` - 只修改了文档
 * `style` - 对代码格式进行调整，美化。不影响执行逻辑
 * `ref` - 大规模重构(优化屎山但不影响代码执行效果)
 * `perf` - 优化性能的改动
 * `test` - 添加或修复测试
 * `build` - 影响构建系统和外部依赖的更改
 * `ci` - ci和脚本修改
 * `chore` - 不修改代码和测试的杂项更改
 * `revert` - 撤销某次更改

`scope` - 可选，代表更改的位置或某个功能，比如更改了设置功能，那么此项应为 `settings` 此项全部字母小写，用 `-` 连接，例如 `file-reader`

`subject` - 简要描述更改内容，中英不限

`body` (多行) - 用于详细说明改动内容

`footer` (多行) - 可选，用于描述附加信息比如关联某个issue

### 1.2. 示例

如果你添加了下载模块，那么提交信息应该是这样的:

```
feat(download): 添加下载模块
```

为某处代码添加注释，空行，便于理解。那么应该为:

```
style: 为xxx添加注释
```
